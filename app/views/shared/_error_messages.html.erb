<% if object.errors.any? %>
  <div class="alert alert-danger">
    <b>The form contains <%= pluralize(object.errors.count, "error") %>.</b></br>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
